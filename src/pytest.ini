[pytest]
# Verzeichnis, in dem pytest nach Tests sucht
testpaths = tests

# Muster für Testdateien
python_files = test_*.py *_test.py

# Muster für Testfunktionen
python_functions = test_*

# Optional: Marker
markers =
    slow: mark a test as slow

# Optional: Zusätzliche Optionen
addopts = -v